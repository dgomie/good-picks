
    <div class="table-row">
        <div class="table-cell p-2 text-left">{{this.music.title}}</div>
        <div class="table-cell p-2 text-left">{{this.music.artist.name}}</div>
        <div class="table-cell p-2 text-left">{{this.music.album}}</div>
        <div class="table-cell p-2 text-left">{{this.rating}}</div>
    </div> 



